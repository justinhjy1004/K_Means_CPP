#ifndef PARSER_HPP
#define PARSER_HPP

#include <fstream>
#include <iostream>
#include <vector>
#include <regex>

using namespace std;

vector<vector<double>> parse_csv(string file_name);

#endif